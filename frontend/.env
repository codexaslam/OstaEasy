# Frontend Environment Variables (Base Configuration)
# Vite prefix: VITE_ is required for Vite to expose variables to the client

# Default/Fallback API Base URL (will be overridden by environment-specific files)
VITE_API_BASE_URL=http://localhost:8000

# Stripe Publishable Key (safe to expose on frontend)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_01QZ55AeQfGutsrsRjjkToqz

# Default settings
VITE_DEBUG=true
VITE_ENVIRONMENT=development

# Note: This file provides defaults. 
# .env.local overrides for local development
# .env.production overrides for production builds
